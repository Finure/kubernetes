apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: app-backend
  namespace: ai
spec:
  interval: 1m
  chart:
    spec:
      chart: k8s/helm-charts/app-backend
      sourceRef:
        kind: GitRepository
        name: app-backend
        namespace: ai
