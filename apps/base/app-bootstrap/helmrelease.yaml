apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: app-bootstrap
  namespace: ai
spec:
  interval: 1m
  chart:
    spec:
      chart: k8s/helm-charts/app-bootstrap
      sourceRef:
        kind: GitRepository
        name: app-bootstrap
        namespace: ai
