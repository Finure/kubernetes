apiVersion: v1
kind: Pod
metadata:
  name: zxpvc-test-pod
spec:
  containers:
    - name: app
      image: alpine/semver:7.5.4
      command: ["sh", "-c", "sleep 3600"]
      volumeMounts:
        - name: mypvc
          mountPath: /mnt/data
  volumes:
    - name: mypvc
      persistentVolumeClaim:
        claimName: pvc-2e7fb5a8ff
  restartPolicy: Never
