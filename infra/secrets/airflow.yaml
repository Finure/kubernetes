apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: airflow
  namespace: airflow
spec:
  refreshInterval: "1m"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: airflow
  dataFrom:
  - extract:
      key: infra/data/airflow
