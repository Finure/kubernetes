apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: atlantis-github
  namespace: tools
spec:
  refreshInterval: "1m"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: atlantis-github
  dataFrom:
  - extract:
      key: infra/data/atlantis-github
