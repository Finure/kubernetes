apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: webapp
  namespace: cnpg-database
spec:
  refreshInterval: "1m"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: webapp
  dataFrom:
  - extract:
      key: infra/data/webapp
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: webapp
  namespace: ai
spec:
  refreshInterval: "1m"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: webapp
  dataFrom:
  - extract:
      key: infra/data/webapp
