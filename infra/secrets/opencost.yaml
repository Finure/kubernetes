apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: opencost
  namespace: monitoring
spec:
  refreshInterval: "1m"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: opencost
  dataFrom:
  - extract:
      key: infra/data/opencost
